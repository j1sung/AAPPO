using UnityEngine;

public class EnvController : MonoBehaviour
{
    public Player_Agent Agent_A;
    public Player_Agent Agent_B;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
